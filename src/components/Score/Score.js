/**
 * takes the values from Timer and Guesses
 * performs a calculation on these values and stores in setScore(). 
 * displays the current score in a scoreboard
 * score is available to save to mongoDB?
 * props answer and time left
 */

function Score() {
  return ( <div></div> );
}

export default Score;